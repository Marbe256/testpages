# 📐 $(n+k)^\alpha$ の差分操作の算出（一般化）

## 1. 0階差分（元の式）

$\alpha$ を自然数と仮定し、二項定理を用いて展開します。

$$(n+k)^\alpha = \sum_{r=0}^{\alpha} \binom{\alpha}{r} n^{\alpha-r} k^{r} = \binom{\alpha}{0}n^\alpha k^0 + \binom{\alpha}{1}n^{\alpha-1} k^1 + \dots + \binom{\alpha}{\alpha}n^0 k^\alpha$$

| $k$ | 左辺 | 右辺 |
| :---: | :--- | :--- |
| **0** | $$(n+0)^\alpha$$|$$\binom{\alpha}{0}n^\alpha$$ |
| **1** | $$(n+1)^\alpha$$|$$\sum_{r=0}^{\alpha} \binom{\alpha}{r} n^{\alpha-r} 1^r$$ |
| **2** | $$(n+2)^\alpha$$|$$\sum_{r=0}^{\alpha} \binom{\alpha}{r} n^{\alpha-r} 2^r$$ |
| **3** | $$(n+3)^\alpha$$|$$\sum_{r=0}^{\alpha} \binom{\alpha}{r} n^{\alpha-r} 3^r$$ |

---

## 2. 1階差分 ($\Delta$)

$$
(n+k+1)^\alpha - (n+k)^\alpha = \sum_{r=1}^{\alpha} \binom{\alpha}{r} n^{\alpha-r} \left[ (k+1)^{r} - k^{r} \right]
$$

| $k$ | 左辺 | 右辺 |
| :---: | :--- | :--- |
| **0** | $$(n+1)^\alpha - (n+0)^\alpha$$|$$\sum_{r=1}^{\alpha} \binom{\alpha}{r} n^{\alpha-r} (1^r - 0^r)$$ |
| **1** | $$(n+2)^\alpha - (n+1)^\alpha$$|$$\sum_{r=1}^{\alpha} \binom{\alpha}{r} n^{\alpha-r} (2^r - 1^r)$$ |
| **2** | $$(n+3)^\alpha - (n+2)^\alpha$$|$$\sum_{r=1}^{\alpha} \binom{\alpha}{r} n^{\alpha-r} (3^r - 2^r)$$ |

---

## 3. $m$階差分 ($\Delta^m$)

一般に、 $m$階差分は以下のように表されます。

$$
\Delta^m (n+k)^\alpha = \sum_{j=0}^{m} \binom{m}{j} (-1)^{m-j} (n+k+j)^\alpha
$$

また、展開式を用いた表現では、最高次の項に着目すると、階差をとるごとに次数が1つ下がります。

### 例： $\alpha=3$ における 2階差分 ($m=2$)

上記の一般式に $\alpha=3, m=2$ を代入し、 $n+k=x$ として展開します。

$$
\begin{aligned}
\Delta^2 x^3 &= \sum_{j=0}^{2} \binom{2}{j} (-1)^{2-j} (x+j)^3 \\
&= \binom{2}{0}(-1)^2 x^3 + \binom{2}{1}(-1)^1(x+1)^3 + \binom{2}{2}(-1)^0(x+2)^3 \\
&= x^3 - 2(x+1)^3 + (x+2)^3 \\
&= x^3 - 2(x^3 + 3x^2 + 3x + 1) + (x^3 + 6x^2 + 12x + 8) \\
&= (1-2+1)x^3 + (-6+6)x^2 + (-6+12)x + (-2+8) \\
&= 6x + 6
\end{aligned}
$$

結果は $x$ の1次式となります。

### 例： $\alpha=4$ における 3階差分 ($m=3$)

同様に、 $\alpha=4, m=3$ を代入し、 $n+k=x$ として展開します。

$$
\begin{aligned}
\Delta^3 x^4 &= \sum_{j=0}^{3} \binom{3}{j} (-1)^{3-j} (x+j)^4 \\
&= \binom{3}{0}(-1)^3 x^4 + \binom{3}{1}(-1)^2(x+1)^4 + \binom{3}{2}(-1)^1(x+2)^4 + \binom{3}{3}(-1)^0(x+3)^4 \\
&= -x^4 + 3(x+1)^4 - 3(x+2)^4 + (x+3)^4 \\
&= -x^4 + 3(x^4 + 4x^3 + 6x^2 + 4x + 1) - 3(x^4 + 8x^3 + 24x^2 + 32x + 16) + (x^4 + 12x^3 + 54x^2 + 108x + 81) \\
&= (-1+3-3+1)x^4 + (12-24+12)x^3 + (18-72+54)x^2 + (12-96+108)x + (3-48+81) \\
&= 24x + 36
\end{aligned}
$$

結果は $x$ の1次式となります。

---

## 4. 最終結果（ $\alpha$階差分）

$\alpha$ が自然数の場合、 $\alpha$ 階差分をとると
$$
\Delta^\alpha_k = \alpha!
$$

| 差分 | 展開された交代和の式 $=$ 最終定数 |
| :---: | :--- |
| **$\Delta^\alpha (0^\alpha)$** | $$\sum_{j=0}^{\alpha} \binom{\alpha}{j} (-1)^{\alpha-j} j^{\alpha} = \alpha!$$ |

$\alpha$ が自然数の場合、 $\alpha$ 階差分をとると定数 $\alpha!$ になります。

### 証明（最高次の項に着目）

多項式 $P(x) = a_n x^n + a_{n-1} x^{n-1} + \dots$ （ $a_n \neq 0$ ）の差分 $\Delta P(x)$ を考えます。

$$
\begin{aligned}
\Delta P(x) &= P(x+1) - P(x) \\
&= \left( a_n (x+1)^n + a_{n-1} (x+1)^{n-1} + \dots \right) - \left( a_n x^n + a_{n-1} x^{n-1} + \dots \right) \\
&= a_n \left( x^n + \binom{n}{1} x^{n-1} + \binom{n}{2} x^{n-2} + \dots \right) + a_{n-1} (x+1)^{n-1} + \dots - \left( a_n x^n + a_{n-1} x^{n-1} + \dots \right) \\
&= \left( a_n x^n + a_n n x^{n-1} + \dots \right) - a_n x^n + (\text{$x^{n-1}$ より低次の項}) \\
&= a_n n x^{n-1} + (\text{$x^{n-2}$ 以下の項})
\end{aligned}
$$

これにより、差分をとる操作 $\Delta$ は、多項式の次数を1つ下げ、最高次の係数を元の次数倍することがわかります。

$f(x) = x^\alpha$ （最高次の係数は 1）に対してこの操作を $\alpha$ 回繰り返すと、

1. 1回目: $\Delta x^\alpha = \alpha x^{\alpha-1} + \dots$
2. 2回目: $\Delta^2 x^\alpha = \alpha (\alpha-1) x^{\alpha-2} + \dots$
3. ...
4. $\alpha$回目: $\Delta^\alpha x^\alpha = \alpha (\alpha-1) \cdots 1 \cdot x^0 = \alpha!$

したがって、多項式の性質から $\alpha$ 階差分の値は $\alpha!$ となります。

$$
\Delta^\alpha x^\alpha = \alpha!
$$

一方で、差分の定義から $\Delta^\alpha x^\alpha$ は以下の交代和で表されます（ $n=0, k=0$ として $x=0$ の場合を考えると）。

$$
\Delta^\alpha 0^\alpha = \sum_{j=0}^{\alpha} \binom{\alpha}{j} (-1)^{\alpha-j} j^{\alpha}
$$

よって、この2つの結果を合わせることで、交代和が $\alpha!$ に等しいことが証明されます。

$$
\sum_{j=0}^{\alpha} \binom{\alpha}{j} (-1)^{\alpha-j} j^{\alpha} = \alpha!
$$

---
